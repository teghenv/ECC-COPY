interface:
  display_name: "Sandbox Testing"
  short_description: "Route clean-user tests through ECC sandboxes"
  default_prompt: "Use $sandbox-testing to run a sandbox manifest; compose it with $terminal-opener when visible or interactive review is appropriate."
